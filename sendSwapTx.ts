export async function sendSwapTx(
  wallet: any,
  connection: any,
  fromMint: string,
  toMint: string,
  amount: number
) {
  console.log('Pretending to send swap transaction...');
  return { success: true };
}
